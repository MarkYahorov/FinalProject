<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalprogect">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProgect">
        <activity android:name=".presrntation.mainScreen.AddTask.SingleTaskActivity"></activity>
        <activity android:name=".presrntation.mainScreen.AddTask.RegularActivity" />
        <activity android:name=".presrntation.mainScreen.ChooseTask.ChooseTask" />
        <activity android:name=".presrntation.startScreen.StartActivity" />
        <activity android:name=".presrntation.mainScreen.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>